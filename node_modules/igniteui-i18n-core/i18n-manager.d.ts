import { DateFormatter } from './formatters/date.formatter.js';
import { DisplayNamesFormatter } from './formatters/display-names.formatter.js';
import { LocaleFormatter } from './formatters/locale.formatter.js';
import { NumberFormatter } from './formatters/number.formatter.js';
import type { IResourceStrings } from './i18n/interfaces/resources.interface.js';
import { type CustomEventListenerObject, type I18nManagerEventMap, I18nManagerEventTarget, type IIgI18nManager } from './i18n-manager.interfaces.js';
export declare class I18nManager extends I18nManagerEventTarget implements IIgI18nManager {
    private readonly _defaultLang;
    readonly defaultLocale = "en-US";
    private static _instance;
    private _currentLocale;
    private readonly _formatters;
    private readonly _resourcesMap;
    private readonly _rootObserver;
    get currentLocale(): string;
    get localeFormatter(): LocaleFormatter;
    get dateFormatter(): DateFormatter;
    get numberFormatter(): NumberFormatter;
    get displayNamesFormatter(): DisplayNamesFormatter;
    static get instance(): I18nManager;
    private constructor();
    /**
     * Sets up a function that will be called whenever the specified event is delivered to the target.
     * Currently supported events: onResourceChange
     * Note: Make sure to bind to events only in browser environment or emulated one. Anything else like server environment does not need to have events.
     * @param type Name of the event
     * @param listener The function to be called when event is triggered.
     * @param options
     */
    addEventListener<K extends keyof I18nManagerEventMap>(type: K, listener: ((evt: I18nManagerEventMap[K]) => void) | CustomEventListenerObject<I18nManagerEventMap[K]>, options?: boolean | EventListenerOptions): void;
    /**
     * Register resource for a locale. Can be the current locale as well or a new one.
     */
    registerI18n(resources: IResourceStrings, locale: string): void;
    /**
     * Set current locale across all components.
     */
    setCurrentI18n(locale: string): void;
    /**
     * Get the current resource string for all components in a single object.
     */
    getCurrentResourceStrings(locale?: string): IResourceStrings;
    private getLocaleObject;
    private getDefaultResources;
    private getDefaultForCategory;
    private getResourcesPerLocale;
    private setResourcesPerLocale;
    private updateDefaultResources;
    private triggerResourceChange;
    private htmlElementObserve;
}
/**
 * Gets in the i18nManager instance.
 * @internal
 */
export declare function getI18nManager(): I18nManager;
export declare function getDateFormatter(): DateFormatter;
export declare function getNumberFormatter(): NumberFormatter;
export declare function getDisplayNamesFormatter(): DisplayNamesFormatter;
/**
 * Register resources for a specific locale.
 * @param resourceStrings Object containing the translated resource strings.
 * @param locale The name of a locale. A string using the BCP 47 language tag. If not a valid one, will register to the default 'en-US' locale.
 */
export declare function registerI18n(resourceStrings: IResourceStrings, locale: string): void;
/**
 * Set the current locale of all Ignite UI components.
 * @param locale The name of the locale. A string using the BCP 47 language tag.
 */
export declare function setCurrentI18n(locale: string): void;
export declare function getCurrentI18n(): string;
export declare function getCurrentResourceStrings(): IResourceStrings;
