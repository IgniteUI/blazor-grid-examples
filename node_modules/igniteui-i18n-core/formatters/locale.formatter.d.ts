import { BaseFormatter } from './base.formatter.js';
export declare class LocaleFormatter extends BaseFormatter<Intl.Locale, Intl.LocaleOptions> {
    constructor(defaultLocale: string);
    static equalLocaleLanguages(leftLocale: Intl.Locale, rightLocale: Intl.Locale): boolean;
}
