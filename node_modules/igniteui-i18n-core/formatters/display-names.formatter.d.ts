import { BaseFormatter } from './base.formatter.js';
import type { DateFormatter } from './date.formatter.js';
export declare class DisplayNamesFormatter extends BaseFormatter<Intl.DisplayNames, Intl.DisplayNamesOptions> {
    private dateFormatter;
    constructor(defaultLocale: string, dateFormatter: DateFormatter);
    getWeekLabel(locale?: string, options?: Partial<Intl.DisplayNamesOptions>): string;
}
